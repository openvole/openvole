# Generated by setup.sh — per-machine, never commit
spaces.json
spaces/*/.openvole/
spaces/*/.env
spaces/*/node_modules/
spaces/*/package-lock.json

# The repo root .gitignore ignores vole.config.json globally — but here these
# ARE the example templates, so force them back into version control.
!spaces/*/vole.config.json
