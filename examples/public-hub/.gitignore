# Generated by setup.sh / runtime — per-machine, never commit
spaces.json
spaces/*/.openvole/
spaces/*/.env
spaces/*/node_modules/
spaces/*/package-lock.json

# The repo root .gitignore ignores vole.config.json globally — but this is the
# example template, so force it back into version control.
!spaces/*/vole.config.json
